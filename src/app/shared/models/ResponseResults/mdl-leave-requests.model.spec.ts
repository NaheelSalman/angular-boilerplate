import { mdlLeaveRequests } from './mdl-leave-requests.model';

describe('mdlLeaveRequests', () => {
  it('should create an instance', () => {
    expect(new mdlLeaveRequests()).toBeTruthy();
  });
});
