import { mdl_ApprovalRequests } from './mdl-approval-requests.model';

describe('MdlApprovalRequests', () => {
  it('should create an instance', () => {
    expect(new mdl_ApprovalRequests()).toBeTruthy();
  });
});
