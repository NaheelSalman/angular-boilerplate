import { mdl_OvertimeRequest } from './mdl-overtime-request.model';

describe('mdl_OvertimeRequest', () => {
  it('should create an instance', () => {
    expect(new mdl_OvertimeRequest()).toBeTruthy();
  });
});
