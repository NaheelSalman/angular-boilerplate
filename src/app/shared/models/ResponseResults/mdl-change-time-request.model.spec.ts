import { mdl_ChangeTimeRequest } from './mdl-change-time-request.model';

describe('MdlChangeTimeRequest', () => {
  it('should create an instance', () => {
    expect(new mdl_ChangeTimeRequest()).toBeTruthy();
  });
});
