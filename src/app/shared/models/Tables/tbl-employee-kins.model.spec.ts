import { tbl_EmployeeKins } from './tbl-employee-kins.model';

describe('tbl_EmployeeKins', () => {
  it('should create an instance', () => {
    expect(new tbl_EmployeeKins()).toBeTruthy();
  });
});
