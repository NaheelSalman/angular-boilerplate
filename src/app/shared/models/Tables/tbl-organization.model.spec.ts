import { tbl_Organization } from './tbl-organization.model';

describe('tbl_Organization', () => {
  it('should create an instance', () => {
    expect(new tbl_Organization()).toBeTruthy();
  });
});
