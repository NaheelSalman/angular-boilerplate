import { mdl_AttendanceOverviewParams } from './mdl-attendance-overview-params.model';

describe('MdlAttendanceOverviewParams', () => {
  it('should create an instance', () => {
    expect(new mdl_AttendanceOverviewParams()).toBeTruthy();
  });
});
