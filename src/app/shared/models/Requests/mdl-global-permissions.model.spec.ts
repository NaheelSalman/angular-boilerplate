import { mdl_GlobalPermissions } from './mdl-global-permissions.model';

describe('mdl_GlobalPermissions', () => {
  it('should create an instance', () => {
    expect(new mdl_GlobalPermissions()).toBeTruthy();
  });
});
