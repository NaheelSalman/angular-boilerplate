import { mdl_EmployeeLeaves } from './mld-employee-leaves.model';

describe('mdl_EmployeeLeaves', () => {
  it('should create an instance', () => {
    expect(new mdl_EmployeeLeaves()).toBeTruthy();
  });
});
