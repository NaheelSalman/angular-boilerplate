import { mdl_Authenticate } from './mdl-authenticate.model';

describe('mdl_Authenticate', () => {
  it('should create an instance', () => {
    expect(new mdl_Authenticate()).toBeTruthy();
  });
});
