import { req_AttendanceData } from './req-attendance-data.model';

describe('req_AttendanceData', () => {
  it('should create an instance', () => {
    expect(new req_AttendanceData()).toBeTruthy();
  });
});
